'use strict';

describe('myApp.waiting module', function() {

  beforeEach(module('myApp.waiting'));

  describe('waiting controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var waitingCtrl = $controller('WaitingCtrl');
      expect(waitingCtrl).toBeDefined();
    }));

  });
});